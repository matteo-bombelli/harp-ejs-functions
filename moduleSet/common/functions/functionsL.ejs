<%
var MBlibraries = false;
if(typeof mbfunction !== "undefined"){
    if(typeof mbfunction.libraries !== "undefined"){
        MBlibraries = true;
        
		var MBneedToInstall = true;

		// check the version of MB if it is not definited or changed will reload MB functions libraries.
		/*
		if(typeof MBversion !== "undefined" ){
			if (MBversion == partial("version.ejs"){
				// MBneedToInstall = false; 
				// in production i would comment it (checks the version of MB if it is different) and leave the false 
			}
		}
		*/
		if(MBneedToInstall){
            MB={};
            for(var i in mbfunction.libraries){
                partial("lib/"+mbfunction.libraries[i]);
            }
		}
    }
}

// not so usefull but can be a good choiche for old machines...
/*
if(!MBlibraries){
    if(typeof MB === "undefined"){
        partial(functions.ejs);
    }
}
*/
%>